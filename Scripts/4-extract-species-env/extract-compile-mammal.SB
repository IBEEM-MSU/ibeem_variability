#!/bin/bash
n="extractSpecies" # job name

# Note that 52 is the number of pieces the MAM IDs were split up into. 
# Should probably not hardcode this...
for i in {1..52}
do
  sbatch --job-name=$n --output=$n.$i.SLURMout --export=fileName=MAMIDsPiece-$i.rda extract-mammal.SB
done
